# 共通設計書

## 1. 概要

### 1.1 目的
全画面で共通して使用されるコンポーネント、デザインパターン、動作仕様を定義する。

### 1.2 適用範囲
- メイン画面（イベント一覧）
- サイドメニュー（フィルター画面）
- イベント作成・編集画面
- イベント履歴画面
- 設定画面
- その他すべての画面

## 2. ローディングオーバーレイ

### 2.1 目的
APIリクエスト中に画面全体をオーバーレイで覆い、以下を実現する：
- **2重クリック防止**: ユーザーが複数回ボタンをクリックするのを防ぐ
- **処理中の視覚的フィードバック**: 処理が進行中であることをユーザーに明示
- **UX向上**: 待機時間の不安を軽減

### 2.2 UI/UXデザイン

#### 基本デザイン

```
┌─────────────────────────────────────┐
│                                     │
│                                     │
│                                     │
│          ┌───────────┐              │
│          │           │              │
│          │   ⟳      │              │ ← スピナー
│          │  読込中... │              │
│          │           │              │
│          └───────────┘              │
│                                     │
│                                     │
│                                     │
└─────────────────────────────────────┘
       ↑ 半透明オーバーレイ
```

#### スタイル仕様

**オーバーレイ:**
- 背景色: rgba(0, 0, 0, 0.3)（黒30%透明度）
- z-index: 9999
- 位置: fixed（画面全体をカバー）
- カーソル: wait

**ローディングコンテナ:**
- 背景色: 白 (#FFFFFF)
- ボーダーラジアス: 12px
- パディング: 24px
- シャドウ: 0 4px 6px rgba(0, 0, 0, 0.1)
- 最大幅: 200px
- 位置: 画面中央（vertical & horizontal center）

**スピナー:**
- タイプ: 回転アニメーション
- サイズ: 40x40px
- 色: プライマリーカラー (#3B82F6)
- アニメーション: 1秒で1回転

**テキスト:**
- 内容: "読込中..."
- フォントサイズ: 14px
- フォントウェイト: 500（Medium）
- 色: グレー (#6B7280)
- マージン上: 12px

### 2.3 動作仕様

#### 表示タイミング

1. **APIリクエスト開始時**
   - `$fetch()` などのAPIリクエスト関数実行直前
   - ローディングオーバーレイを表示

2. **最小表示時間**
   - 300ms（チラつき防止）
   - APIが300ms未満で完了しても、最低300ms表示する

#### 非表示タイミング

1. **APIレスポンス受信時**
   - 成功（200, 201など）
   - 失敗（400, 500など）
   - いずれの場合もオーバーレイを非表示

2. **タイムアウト時**
   - 30秒経過後、自動的に非表示
   - エラーメッセージを表示

#### フロー図

```
[ボタンクリック]
      ↓
[ローディング表示]
      ↓
[APIリクエスト開始]
      ↓
    ┌─────────────────┐
    │ 成功 │ 失敗 │ タイムアウト │
    └──┬───┴───┬──┴──────┬─────┘
       ↓       ↓         ↓
[ローディング非表示]
       ↓       ↓         ↓
   [成功処理] [エラー表示] [タイムアウト表示]
```

### 2.4 実装方法

#### グローバルステート管理

```typescript
// stores/loading.ts
export const useLoadingStore = defineStore('loading', () => {
  const isLoading = ref(false);
  const loadingCount = ref(0); // 複数同時リクエスト対応
  const minDisplayTime = 300; // 最小表示時間（ms）
  const timeout = 30000; // タイムアウト（ms）
  
  let showTime = 0;
  let timeoutId: NodeJS.Timeout | null = null;
  
  // ローディング開始
  const startLoading = () => {
    loadingCount.value++;
    
    if (!isLoading.value) {
      isLoading.value = true;
      showTime = Date.now();
      
      // タイムアウト設定
      timeoutId = setTimeout(() => {
        stopLoading();
        // タイムアウトエラー表示
        console.error('Request timeout');
      }, timeout);
    }
  };
  
  // ローディング終了
  const stopLoading = async () => {
    loadingCount.value = Math.max(0, loadingCount.value - 1);
    
    if (loadingCount.value === 0 && isLoading.value) {
      // 最小表示時間を確保
      const elapsed = Date.now() - showTime;
      const remaining = Math.max(0, minDisplayTime - elapsed);
      
      if (remaining > 0) {
        await new Promise(resolve => setTimeout(resolve, remaining));
      }
      
      isLoading.value = false;
      
      // タイムアウトクリア
      if (timeoutId) {
        clearTimeout(timeoutId);
        timeoutId = null;
      }
    }
  };
  
  // 強制終了（エラー時など）
  const forceStopLoading = () => {
    loadingCount.value = 0;
    isLoading.value = false;
    if (timeoutId) {
      clearTimeout(timeoutId);
      timeoutId = null;
    }
  };
  
  return {
    isLoading: readonly(isLoading),
    startLoading,
    stopLoading,
    forceStopLoading,
  };
});
```

#### Composable（推奨）

```typescript
// composables/useApiRequest.ts
export const useApiRequest = () => {
  const loadingStore = useLoadingStore();
  
  // API リクエストをラップ
  const apiRequest = async <T>(
    requestFn: () => Promise<T>,
    options?: {
      showLoading?: boolean; // デフォルト: true
      errorHandler?: (error: any) => void;
    }
  ): Promise<T | null> => {
    const showLoading = options?.showLoading ?? true;
    
    try {
      if (showLoading) {
        loadingStore.startLoading();
      }
      
      const result = await requestFn();
      return result;
    } catch (error) {
      console.error('API request failed:', error);
      
      if (options?.errorHandler) {
        options.errorHandler(error);
      } else {
        // デフォルトエラーハンドリング
        showToast('エラーが発生しました', 'error');
      }
      
      return null;
    } finally {
      if (showLoading) {
        await loadingStore.stopLoading();
      }
    }
  };
  
  return {
    apiRequest,
  };
};
```

#### グローバルコンポーネント

```vue
<!-- components/LoadingOverlay.vue -->
<template>
  <Teleport to="body">
    <Transition name="fade">
      <div
        v-if="loadingStore.isLoading"
        class="fixed inset-0 z-[9999] flex items-center justify-center bg-black/30"
        @click.prevent
        @touchstart.prevent
      >
        <div
          class="flex flex-col items-center rounded-xl bg-white p-6 shadow-lg"
          role="status"
          aria-live="polite"
          aria-label="読み込み中"
        >
          <!-- スピナー -->
          <div
            class="h-10 w-10 animate-spin rounded-full border-4 border-gray-200 border-t-blue-500"
          />
          
          <!-- テキスト -->
          <p class="mt-3 text-sm font-medium text-gray-600">
            読込中...
          </p>
        </div>
      </div>
    </Transition>
  </Teleport>
</template>

<script setup lang="ts">
const loadingStore = useLoadingStore();
</script>

<style scoped>
.fade-enter-active,
.fade-leave-active {
  transition: opacity 0.2s ease;
}

.fade-enter-from,
.fade-leave-to {
  opacity: 0;
}
</style>
```

### 2.5 使用例

#### パターン1: 通常のAPIリクエスト

```typescript
// pages/index.vue
const { apiRequest } = useApiRequest();

const loadEvents = async () => {
  const result = await apiRequest(
    () => $fetch('/events')
  );
  
  if (result) {
    events.value = result.data.events;
  }
};
```

#### パターン2: カスタムエラーハンドリング

```typescript
// pages/events/[id]/edit.vue
const { apiRequest } = useApiRequest();

const updateEvent = async () => {
  const result = await apiRequest(
    () => $fetch(`/events/${eventId}`, {
      method: 'PUT',
      body: eventData.value,
    }),
    {
      errorHandler: (error) => {
        if (error.statusCode === 404) {
          showToast('イベントが見つかりません', 'error');
          navigateTo('/');
        } else {
          showToast('更新に失敗しました', 'error');
        }
      },
    }
  );
  
  if (result) {
    showToast('イベントを更新しました', 'success');
    navigateTo('/');
  }
};
```

#### パターン3: ローディング非表示（バックグラウンド処理）

```typescript
// pages/settings/index.vue
const { apiRequest } = useApiRequest();

// 設定の自動保存（ローディング非表示）
const autoSaveSettings = async () => {
  await apiRequest(
    () => $fetch('/settings', {
      method: 'PATCH',
      body: settings.value,
    }),
    {
      showLoading: false, // ローディング表示しない
    }
  );
};
```

#### パターン4: 複数のAPIリクエスト

```typescript
// pages/events/[id]/history.vue
const { apiRequest } = useApiRequest();

const loadData = async () => {
  // 1回のローディングで複数取得
  const [eventResult, historyResult] = await Promise.all([
    apiRequest(() => $fetch(`/events/${eventId}`)),
    apiRequest(() => $fetch(`/events/${eventId}/history`)),
  ]);
  
  if (eventResult) event.value = eventResult.data.event;
  if (historyResult) histories.value = historyResult.data.histories;
};
```

### 2.6 適用画面

以下のアクションで自動的にローディングオーバーレイを表示：

#### メイン画面
- イベント一覧取得
- イベント削除

#### イベント作成・編集画面
- イベント作成
- イベント更新
- イベント詳細取得（編集時）

#### イベント履歴画面
- イベント詳細取得
- 履歴一覧取得
- 履歴追加
- 履歴更新
- 履歴削除
- イベント削除

#### 設定画面
- 設定取得
- 設定更新（手動保存時のみ）
- CSVエクスポート

### 2.7 例外ケース

#### ローディングを表示しない場合

1. **自動保存処理**
   - 設定の自動保存
   - ドラフトの自動保存

2. **ポーリング処理**
   - 定期的なデータ更新
   - リアルタイム同期

3. **バックグラウンド処理**
   - ログ送信
   - 分析データ送信

これらの場合は `showLoading: false` オプションを使用。

### 2.8 エラーハンドリング

#### ネットワークエラー

```typescript
const { apiRequest } = useApiRequest();

const result = await apiRequest(
  () => $fetch('/events'),
  {
    errorHandler: (error) => {
      if (!navigator.onLine) {
        showToast('インターネット接続を確認してください', 'error');
      } else if (error.name === 'FetchError') {
        showToast('サーバーに接続できません', 'error');
      } else {
        showToast('エラーが発生しました', 'error');
      }
    },
  }
);
```

#### タイムアウト

30秒経過後、自動的にローディングを終了し、エラーメッセージを表示：

```typescript
// stores/loading.ts 内で自動処理
timeoutId = setTimeout(() => {
  stopLoading();
  showToast('リクエストがタイムアウトしました', 'error');
}, timeout);
```

### 2.9 アクセシビリティ

#### ARIA属性

```html
<div
  role="status"
  aria-live="polite"
  aria-label="読み込み中"
>
  <!-- スピナー -->
</div>
```

#### スクリーンリーダー対応
- `aria-live="polite"`: ローディング状態の変化を通知
- `aria-label="読み込み中"`: ローディング中であることを明示
- `role="status"`: ステータス変更を示す

#### キーボード操作
- ローディング中はすべてのインタラクティブ要素を無効化
- Tabキーでのフォーカス移動をブロック

### 2.10 パフォーマンス最適化

#### 最小表示時間（300ms）
チラつきを防止し、UXを向上：

```typescript
const elapsed = Date.now() - showTime;
const remaining = Math.max(0, minDisplayTime - elapsed);

if (remaining > 0) {
  await new Promise(resolve => setTimeout(resolve, remaining));
}
```

#### 複数同時リクエスト対応
`loadingCount` でリクエスト数をカウント：

```typescript
const startLoading = () => {
  loadingCount.value++;
  // 最初のリクエストでのみ表示開始
  if (loadingCount.value === 1) {
    isLoading.value = true;
  }
};

const stopLoading = () => {
  loadingCount.value--;
  // すべてのリクエストが完了したら非表示
  if (loadingCount.value === 0) {
    isLoading.value = false;
  }
};
```

### 2.11 テストケース

#### 単体テスト

```typescript
// tests/unit/useApiRequest.spec.ts
describe('useApiRequest', () => {
  it('成功時にローディングを表示・非表示する', async () => {
    const { apiRequest } = useApiRequest();
    const loadingStore = useLoadingStore();
    
    const promise = apiRequest(() => Promise.resolve({ data: 'test' }));
    expect(loadingStore.isLoading).toBe(true);
    
    await promise;
    expect(loadingStore.isLoading).toBe(false);
  });
  
  it('エラー時にローディングを非表示する', async () => {
    const { apiRequest } = useApiRequest();
    const loadingStore = useLoadingStore();
    
    await apiRequest(() => Promise.reject(new Error('test error')));
    expect(loadingStore.isLoading).toBe(false);
  });
  
  it('最小表示時間（300ms）を確保する', async () => {
    const { apiRequest } = useApiRequest();
    const loadingStore = useLoadingStore();
    
    const startTime = Date.now();
    await apiRequest(() => Promise.resolve({ data: 'test' }));
    const elapsed = Date.now() - startTime;
    
    expect(elapsed).toBeGreaterThanOrEqual(300);
  });
  
  it('30秒後にタイムアウトする', async () => {
    const { apiRequest } = useApiRequest();
    const loadingStore = useLoadingStore();
    
    const promise = apiRequest(
      () => new Promise(resolve => setTimeout(resolve, 40000))
    );
    
    // 30秒待機
    await new Promise(resolve => setTimeout(resolve, 30000));
    expect(loadingStore.isLoading).toBe(false);
  });
});
```

#### E2Eテスト

```typescript
// tests/e2e/loading.spec.ts
test('イベント作成時にローディングが表示される', async ({ page }) => {
  await page.goto('/events/new');
  
  // フォーム入力
  await page.fill('input[name="name"]', 'テストイベント');
  
  // 完了ボタンクリック
  await page.click('button[type="submit"]');
  
  // ローディングオーバーレイが表示される
  await expect(page.locator('[role="status"]')).toBeVisible();
  
  // ローディングが消える
  await expect(page.locator('[role="status"]')).not.toBeVisible();
  
  // メイン画面に遷移
  await expect(page).toHaveURL('/');
});
```

## 3. トーストメッセージ

### 3.1 目的
ユーザーにフィードバックを提供するための一時的な通知メッセージ。

### 3.2 デザイン仕様

#### 成功メッセージ
```
┌──────────────────────────┐
│ ✓  イベントを作成しました  │ ← 緑色背景
└──────────────────────────┘
```

#### エラーメッセージ
```
┌──────────────────────────┐
│ ✗  エラーが発生しました    │ ← 赤色背景
└──────────────────────────┘
```

#### 情報メッセージ
```
┌──────────────────────────┐
│ ℹ  処理を開始しました      │ ← 青色背景
└──────────────────────────┘
```

### 3.3 表示仕様

- **位置**: 画面上部中央
- **表示時間**: 3秒（自動消滅）
- **アニメーション**: スライドダウン + フェードイン
- **z-index**: 10000（ローディングより上）

### 3.4 実装

Nuxt UI の `useToast()` を使用：

```typescript
const toast = useToast();

// 成功
toast.add({
  title: 'イベントを作成しました',
  color: 'green',
  icon: 'i-heroicons-check-circle',
});

// エラー
toast.add({
  title: 'エラーが発生しました',
  color: 'red',
  icon: 'i-heroicons-x-circle',
});

// 情報
toast.add({
  title: '処理を開始しました',
  color: 'blue',
  icon: 'i-heroicons-information-circle',
});
```

## 4. ユーザー情報管理

### 4.1 目的
ログインユーザーのニックネームなどの情報をPiniaストアで管理し、アプリ全体で利用可能にする。

### 4.2 初回アクセス時の動作

#### フロー

```
[アプリ起動]
      ↓
[Piniaストアをチェック]
      ↓
  ┌─────────────┐
  │ニックネームあり? │
  └──┬─────┬──┘
     YES    NO
      ↓      ↓
  [表示]  [GET /auth/me]
              ↓
         [ニックネーム取得]
              ↓
         [Piniaに保存]
              ↓
            [表示]
```

### 4.3 実装方法

#### Piniaストア

```typescript
// stores/user.ts
export const useUserStore = defineStore('user', () => {
  const user = ref<User | null>(null);
  
  interface User {
    id: number;
    email: string;
    nickname: string;
    createdAt: string;
    updatedAt: string;
  }
  
  // ユーザー情報を取得（必要に応じてAPI呼び出し）
  const fetchUser = async () => {
    // 既にユーザー情報がある場合はスキップ
    if (user.value?.nickname) {
      return user.value;
    }
    
    try {
      const response = await $fetch<{
        success: boolean;
        data: { user: User };
      }>('/auth/me', {
        headers: {
          Authorization: `Bearer ${getAccessToken()}`,
        },
      });
      
      if (response.success) {
        user.value = response.data.user;
        return user.value;
      }
    } catch (error) {
      console.error('Failed to fetch user info:', error);
      return null;
    }
  };
  
  // ユーザー情報を設定（ログイン時に使用）
  const setUser = (userData: User) => {
    user.value = userData;
  };
  
  // ユーザー情報をクリア（ログアウト時に使用）
  const clearUser = () => {
    user.value = null;
  };
  
  // ニックネームを取得
  const getNickname = computed(() => user.value?.nickname || '');
  
  return {
    user: readonly(user),
    nickname: getNickname,
    fetchUser,
    setUser,
    clearUser,
  };
});
```

#### アクセストークン取得ヘルパー

```typescript
// utils/auth.ts
export const getAccessToken = (): string | null => {
  // CookieまたはlocalStorageからトークンを取得
  const token = useCookie('access_token').value;
  return token || null;
};
```

#### アプリケーション初期化時の処理

```typescript
// app.vue または layouts/default.vue
<script setup lang="ts">
const userStore = useUserStore();
const { apiRequest } = useApiRequest();

// アプリ起動時にユーザー情報を取得
onMounted(async () => {
  // ニックネームがない場合のみAPI呼び出し
  if (!userStore.nickname) {
    await apiRequest(
      () => userStore.fetchUser(),
      { showLoading: false } // 初回取得時はローディング非表示
    );
  }
});
</script>
```

### 4.4 各画面での使用例

#### メイン画面

```vue
<script setup lang="ts">
const userStore = useUserStore();

onMounted(async () => {
  // ニックネームが未取得の場合、自動的に取得
  if (!userStore.nickname) {
    await userStore.fetchUser();
  }
});
</script>

<template>
  <div>
    <header class="flex justify-between items-center">
      <h1>最後はいつ？</h1>
      <div v-if="userStore.nickname" class="flex items-center gap-2">
        <span>👤</span>
        <span class="truncate max-w-[80px]">{{ userStore.nickname }}</span>
      </div>
    </header>
  </div>
</template>
```

#### サイドメニュー

```vue
<script setup lang="ts">
const userStore = useUserStore();
</script>

<template>
  <div>
    <div class="text-center">
      <h2>最後はいつ？</h2>
      <div v-if="userStore.nickname" class="flex items-center justify-center gap-2 mt-2">
        <span>👤</span>
        <span class="truncate max-w-[160px]">{{ userStore.nickname }}</span>
      </div>
    </div>
  </div>
</template>
```

### 4.5 ログイン・ログアウト時の処理

#### ログイン成功時

```typescript
// pages/login.vue
const handleLogin = async () => {
  const { apiRequest } = useApiRequest();
  const userStore = useUserStore();
  
  const result = await apiRequest(
    () => $fetch('/auth/login', {
      method: 'POST',
      body: { email: email.value, password: password.value },
    })
  );
  
  if (result?.success) {
    // トークンを保存
    useCookie('access_token').value = result.data.accessToken;
    
    // ユーザー情報をPiniaに保存
    userStore.setUser(result.data.user);
    
    // メイン画面へ遷移
    navigateTo('/');
  }
};
```

#### ログアウト時

```typescript
// pages/settings.vue
const handleLogout = async () => {
  const { apiRequest } = useApiRequest();
  const userStore = useUserStore();
  
  await apiRequest(
    () => $fetch('/auth/logout', { method: 'POST' })
  );
  
  // トークンをクリア
  useCookie('access_token').value = null;
  
  // ユーザー情報をクリア
  userStore.clearUser();
  
  // ログイン画面へ遷移
  navigateTo('/login');
};
```

### 4.6 注意事項

- **初回アクセス時のみAPI呼び出し**: Piniaにニックネームが存在する場合は、API呼び出しをスキップする
- **ページ遷移時の処理**: ルートガード（middleware）を使用して、認証が必要なページでは自動的にユーザー情報を取得
- **エラーハンドリング**: `/auth/me` が失敗した場合（401など）は、ログイン画面にリダイレクト
- **パフォーマンス**: 初回取得時はローディングオーバーレイを表示しない（`showLoading: false`）オプションを使用

## 5. 確認ダイアログ

### 5.1 目的
重要な操作（削除など）の実行前にユーザーに確認を求める。

### 5.2 デザイン仕様

```
┌─────────────────────────────────────┐
│                                     │
│  このイベントを削除しますか？        │
│                                     │
│  "エアコンフィルター掃除"            │
│  およびすべての履歴が削除されます。  │
│  この操作は取り消せません。          │
│                                     │
│  ┌─────────┐  ┌─────────┐          │
│  │キャンセル│  │  削除   │          │
│  └─────────┘  └─────────┘          │
│                                     │
└─────────────────────────────────────┘
```

### 5.3 実装

```typescript
const showConfirmDialog = ref(false);

const handleDelete = async () => {
  showConfirmDialog.value = true;
};

const confirmDelete = async () => {
  const { apiRequest } = useApiRequest();
  
  const result = await apiRequest(
    () => $fetch(`/events/${eventId}`, { method: 'DELETE' })
  );
  
  if (result) {
    showToast('イベントを削除しました', 'success');
    navigateTo('/');
  }
  
  showConfirmDialog.value = false;
};
```

---

**作成日**: 2026/01/16  
**バージョン**: 1.0  
**更新履歴**:
- 2026/01/16: 初版作成（ローディングオーバーレイ設計、ユーザー情報管理追加）
